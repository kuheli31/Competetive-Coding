#include <bits/stdc++.h>
using namespace std;

int solve(int a , int b){
    return a + b;
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int a, b;
    cin >> a >> b;
    cout << solve(a, b) << '\n';

    return 0;
}
